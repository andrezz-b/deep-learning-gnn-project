# @package _global_

defaults:
  - override /trainer: mean-teacher-trainer
  - override /model: mean_teacher_gcn

dataset:
  init:
    splits: [0.64, 0.16, 0.10, 0.10] # 20% labeled


save_model: true
